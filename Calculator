//This program is to calculate the simple interest

#include <stdio.h>
void main()
{
    int P,R,T,SI;
    printf("Enter the principle amount:\n");
    scanf("%d", &P);
    printf("Enter the interest rate per annum:\n");
    scanf("%d", &R);
    printf("Enter the time in years:\n");
    scanf("%d", &T);
    SI=(P*R*T)/100;
    printf("The Simple Interest is: %d\n", SI);

}


Sample Output:
Enter the principle amount: 1000
Enter the interest rate per annum: 100
Enter the time in years: 5
The Simple Interest is: 5000

------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//This program is to calculate the ohm's law

#include <stdio.h>
void main()
{
    int i,r,v;
    printf("Enter the current (I):\n");
    scanf("%d", &i);
    printf("Enter the resistance (R):\n");
    scanf("%d", &r);
    v=i*r;
    printf("The potential difference (V) is: %d", v);
}



Sample Output:
Enter the current (I): 10
Enter the resistance (R): 5
The potential difference (V) is: 50

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
//This program is to calculate the BMI

#include <stdio.h>
int main()
{
    
    float w,h,bmi;
    printf("Enter the weight:\n");
    scanf("%f", &w);
    printf("Enter the height:\n");
    scanf("%f", &h);
    bmi=w/(h*h);
    printf("The BMI is: %f", bmi);
    return 0;
}



Sample Output:
Enter the weight: 65.5
Enter the height: 163.25
The BMI is: 0.002458









