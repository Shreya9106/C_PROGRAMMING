//Program to find the surface area and volume of sphere

#include <stdio.h>
int main()
{
    //This program is to calculate the surface area and the volume of the sphere
    
    int r;
    float pi=3.14, Surface_area, Volume; 
    
    printf("Enter the Radius:\n");
    scanf("%d", &r);
    
    //To calculate the surface area of the sphere
    
    Surface_area = 4*pi*r*r;
    
    printf("The Surface area of the sphere is: %.2f \n", Surface_area);
    
    //To calculate the volume of sphere
    
    Volume = 4/3*pi*r*r*r;
    
    printf("The Volume of the sphere is: %.2f \n", Volume);
    
    return 0;
}



Sample Output:
Enter the Radius: 5
The Surface area of the sphere is: 314.00 
The Volume of the sphere is: 392.50 

-----------------------------------------------------------------------------------------------------------------------------------

//THis program is to claculate the volume of the cylinder

#include <stdio.h>
int main()
{
    int r,h;
    float pi=3.14,vol;
    printf("Enter the radius:\n");
    scanf("%d", &r);
    printf("Enter the height:\n");
    vol=pi*r*r*h;
    printf("The volume of cylinder is: %.2f", vol);
}



Sample Output:
Enter the radius: 3
Enter the height: 7
The volume of cylinder is: 28.26

------------------------------------------------------------------------------------------------------------------------------------

//This program is to calculate the area and circumference of circle

#include <stdio.h>
void main()
{
    int r;
    float pi=3.14, Area, Circumference;
    
    printf("Enter the Radius:\n");
    scanf("%d", &r);
    
    Area = pi*r*r;
    printf("The Area of the Circle is: %.2f \n", Area);
    
    Circumference = 2*pi*r;
    printf("The Circumference of the Circle is: %.2f", Circumference);
    
}


Sample Output:
Enter the Radius: 8
The Area of the Circle is: 200.96 
The Circumference of the Circle is: 50.24

-----------------------------------------------------------------------------------------------------------------------------------

//This program is to calculate the area and perimeter of a 

#include <stdio.h>
void main()
{
    int l, b, Area, Perimeter;
    printf("Enter the length: \n");
    scanf("%d", &l);
    printf("Enter the breadth: \n");
    scanf("%d", &b);
    
    Area = l*b;
    printf("The Area of the Rectangle is: %d \n", Area);
    
    Perimeter = 2*(l+b);
    printf("The Perimeter of the Rectangle is: %d", Perimeter);
}


Sample Output:
Enter the length: 5
Enter the breadth: 6
The Area of the Rectangle is: 30 
The Perimeter of the Rectangle is: 22

-------------------------------------------------------------------------------------------------------------------------------














